# Changes from published v2 to staged v3

- Reorganized the single note into four explicitly labelled parts.
- Preserved the four v2 corrections and scope sections, with the torsion
  issue first and self-contained.
- Added the offered one-log sharpening \(K>\sqrt{M/404}\), including the
  double-exponential crossover caveat and separate conditional-frontier and
  unconditional kernel tiers.
- Added the exact one-element sharpness result for the simultaneous-selector
  threshold, at its stated exhaustive-computation plus hand-proof tier.
- Added the elementary general-group theorems for minima three and four,
  together with the kernel-certified algebraic cores.
- Added Census-52 and its 26 stress rows as exact DRAT-certified computation,
  explicitly not a general theorem. The proof files are not shipped.
- Posed the DoubleCyclic conjecture with finite parity-localization evidence,
  the proved empty-overlap reduction, the open residual-overlap node, and the
  kernel-certified \(C_2\times C_{11}\) projection counterexample.
- Kept the \(p=53\) and \(p=59\) rows at published-v2 tiers because the
  requested \(p=53\) cloud metadata object was absent at staging time.
- Extended `verify.py` with small finite-Abelian minima and the projection
  counterexample.
- Added a self-contained 19-module Lean extraction for the unconditional
  headline and carried over `Dcyc.lean` as a standalone finite-group file.
