/-!
# The concrete `C₁₂` witness

The witness is `{0, 1, 2, 3, 5, 6, 8} ⊆ ZMod 12`.

Its kernel certificate is the `m = 6` instance of
`ArtifactQTargets.cyclicEven_orderedDivergence_satisfiable` in
`CycEven.lean`. The full representation table is independently recomputed by
`verify.py`.
-/
